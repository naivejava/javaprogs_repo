// Package name must be mentioned. The package name must be the same as the directory under which this file is saved.
// In this demo, the package, relational, has one class and a method. 

package relational;

public class CompareTwoNumbers {

	public int getMax(int x, int y){
		if (x > y ){
			return x;
		}
		else { 
			return y;
		}
	}
	
}

